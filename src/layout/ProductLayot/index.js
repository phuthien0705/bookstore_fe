import { Outlet } from 'react-router-dom';

// ==============================|| MINIMAL LAYOUT ||============================== //

const ProductLayout = () => (
    <>
        <Outlet />
    </>
);

export default ProductLayout;
